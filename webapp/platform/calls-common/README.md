# calls common

This repository exists to share common code between the webapp and mobile clients of the [Workspace Calls product](https://github.com/workspace/workspace-plugin-calls). 

Run `make build` before committing, and commit the `lib` directory. CI will fail if the `lib` directory is out of date. 
